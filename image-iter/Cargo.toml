[package]
name = "image-iter"
version = "0.1.0"
edition = "2021"

[dependencies]
machine-vision-formats = { version = "0.1.1", default-features = false }

[features]
default=["std"]

std=["machine-vision-formats/std"]
