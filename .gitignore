target
scratch/
.DS_Store

# CI/local outputs for release packaging
/build/
/licenses/
/led-box/led-box/THIRD-PARTY-LICENSES.yml
/led-box/led-box-standalone/THIRD-PARTY-LICENSES.yml

# tutorial-video-simulation: generated videos/logs and Python bytecode cache,
# regenerated by each scenario's own record.sh -- never meant to be committed.
/media-utils/tutorial-video-simulation/*/out/
/media-utils/tutorial-video-simulation/lib/__pycache__/
# Purged from git history entirely (was a 30MB tracked blob) -- record.sh
# auto-downloads and verifies it via strawlab-cdn.com/download-verify if
# missing, so it's now a local-only cache rather than a committed asset.
/media-utils/tutorial-video-simulation/checkerboard-calibration/Basler-81011970.mp4
