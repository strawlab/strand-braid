[package]
name = "braid-apriltag-types"
version = "0.1.0"
edition = "2024"
description = "AprilTag detection type definitions for the Braid ecosystem"

[dependencies]
serde = { workspace = true, features = ["derive"] }
