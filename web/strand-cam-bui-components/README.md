# strand-cam-bui-components

Reusable [Yew](https://yew.rs/) components for browser user interfaces that
connect to a Strand Camera HTTP server.

## Components

- `CamPreview` displays the camera's receiver-paced firehose video stream on a
  canvas, including annotations supplied by Strand Camera.
