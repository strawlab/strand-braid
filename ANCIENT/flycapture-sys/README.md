# flycapture-sys bindings to the FlyCapture2 C library from Point Grey Research

## building on Windows

    # rustup target add x86_64-pc-windows-msvc
    cargo run --target=x86_64-pc-windows-msvc --example simple
