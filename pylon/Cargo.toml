[package]
name = "pylon"
version = "0.1.0"
authors = ["Andrew Straw <strawman@astraw.com>"]
build = "build.rs"

[dependencies]
failure = "0.1"
failure_derive = "0.1"
log = "0.4"
libc = "0.2"

[build-dependencies]
cc = "1.0"

[dev-dependencies]
env_logger = "0.8"
