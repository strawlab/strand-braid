[package]
name = "braidz-chunked-iter"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
csv = "1.1"
chrono = "0.4.31"

flydra-types = { path = "../../flydra-types" }

[dev-dependencies]
clap = { version = "4", features = ["derive"] }
