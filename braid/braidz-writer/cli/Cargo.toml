[package]
name = "braidz-writer-cli"
version = "0.1.0"
edition = "2024"
description = "CLI tool to convert a Braid data directory into a .braidz file"

[dependencies]
clap.workspace = true
anyhow.workspace = true

braidz-writer = { path = ".." }
