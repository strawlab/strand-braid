[package]
name = "write-debian-changelog"
version = "0.12.0-alpha.5"      # braid release synchronized
edition = "2021"

[dependencies]
chrono = { version = "0.4.23", default-features = false, features = [
    "clock",
    "std",
    "wasmbind",
] }
